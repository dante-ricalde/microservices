spring:
  application:
    name: stores
  cloud:
    config:
      uri:  ${CONFIG_SERVER_URI:${vcap.services.${PREFIX:}configserver.credentials.uri:http://user:password@localhost:8888}}
      #uri1:  ${CONFIG_SERVER_URI:${vcap.services.${PREFIX:}configserver.credentials.uri}:${http://user:password@localhost:8888}}
      #instanceId: ${CONFIG_SERVER_URI:${spring.application.name1:http://user:password@localhost:8888}}